{
  "name": "@webling/embed",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "ENTRY=index vite build && ENTRY=web-component vite build",
    "dev": "vite build --watch"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "typescript": "^5.0.0"
  }
}
