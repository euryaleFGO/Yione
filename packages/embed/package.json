{
  "name": "@webling/embed",
  "version": "0.0.1",
  "description": "嵌入 SDK：Script / Custom Element / iframe 三种形态",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": ["src", "dist"],
  "scripts": {
    "build": "echo 'embed build in M8'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --max-warnings 0 || true"
  },
  "peerDependencies": {
    "@webling/core": "workspace:*",
    "@webling/live2d-kit": "workspace:*",
    "@webling/ui": "workspace:*"
  },
  "devDependencies": {
    "@webling/core": "workspace:*",
    "@webling/live2d-kit": "workspace:*",
    "@webling/ui": "workspace:*",
    "typescript": "~5.6.3",
    "vitest": "^2.1.8"
  }
}
