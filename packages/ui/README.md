# @webling/ui

Vue 组件库。

## 规划组件（M1+）

- `ChatBubble/` — 消息气泡
- `InputBar/` — 输入框 + 发送按钮 + 麦克风
- `AvatarStage/` — 封装 live2d-kit 成 Vue 组件
- `MessageList/` — 虚拟滚动列表
- `EmotionBadge/` — 情绪徽章

## composables

- `useChat` / `useAvatar` / `useAudio`
