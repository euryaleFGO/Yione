#!/usr/bin/env python3
"""Seed 默认形象（玲）。M7 实装；M0 留壳。"""
from __future__ import annotations

import sys


def main() -> int:
    print("seed-characters: M7 里会把玲写进 MongoDB。M0 阶段无操作。")
    return 0


if __name__ == "__main__":
    sys.exit(main())
