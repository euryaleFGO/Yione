---
phase: 02-M7-Character-模块
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - scripts/seed.py
  - apps/web/src/stores/chat.ts
  - apps/web/src/views/CharacterPickerView.vue
  - apps/web/src/views/ChatView.vue
autonomous: false
requirements:
  - M7
must_haves:
  truths:
    - "seed 脚本可初始化默认角色（ling）"
    - "主站可切换不同 Live2D 形象"
    - "切换角色时 WebSocket 连接保持不断"
  artifacts:
    - path: "scripts/seed.py"
      provides: "数据库初始化脚本"
      contains: "seed_characters"
    - path: "apps/web/src/stores/chat.ts"
      provides: "Character 列表管理"
      exports: ["useChatStore"]
    - path: "apps/web/src/views/CharacterPickerView.vue"
      provides: "角色选择 UI"
      contains: "character"
  key_links:
    - from: "apps/web/src/views/ChatView.vue"
      to: "apps/web/src/views/CharacterPickerView.vue"
      via: "router link"
      pattern: "CharacterPickerView"
    - from: "apps/web/src/stores/chat.ts"
      to: "backend/app/routers/characters.py"
      via: "HTTP API"
      pattern: "fetch.*characters"
    - from: "apps/web/src/stores/chat.ts"
      to: "backend/app/ws/chat_ws.py"
      via: "change_character WS event"
      pattern: "change_character"
---

<objective>
实现 seed 脚本初始化默认 Character，以及前端角色切换 UI。

Purpose: 完成 M7 的用户体验部分——用户可以在主站切换不同 Live2D 形象。
Output: 可工作的角色选择 UI + seed 脚本
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-M7-Character-模块/02-CONTEXT.md
@.planning/phases/02-M7-Character-模块/02-01-SUMMARY.md
@.planning/phases/02-M7-Character-模块/02-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/routers/characters.py (created in 02-01):
```python
# GET /api/characters -> CharacterList
# GET /api/characters/{character_id} -> CharacterInfo or 404
```

From backend/app/schemas/character.py (created in 02-01):
```python
class CharacterInfo(BaseModel):
    id: str
    name: str
    avatar_config: dict
    greeting: str
    system_prompt: str
    voice_id: str | None
    enabled: bool
```

From packages/core/src/types/ws.ts:
```typescript
export type ClientEvent =
  | { type: 'user_message'; text: string }
  | { type: 'cancel' }
  | { type: 'ping' }
  | { type: 'change_character'; character_id: string }
  | { type: 'speech_start' }
  | { type: 'speech_end' };
```

From apps/web/src/stores/chat.ts (existing):
```typescript
export const useChatStore = defineStore('chat', () => {
  // ... existing implementation
  return {
    session,
    messages,
    draft,
    connection,
    agentState,
    lastError,
    ensureSession,
    connectSocket,
    submit,
    interrupt,
    disconnect,
    setAvatarControls,
  };
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 seed 脚本初始化默认 Character</name>
  <files>scripts/seed.py</files>
  <behavior>
    - Test: seed_characters() 插入默认 character（ling）
    - Test: 重复执行不会重复插入
    - Test: seed_tenants() 插入 demo tenant
  </behavior>
  <action>
    创建 `scripts/seed.py`，用于初始化 MongoDB 默认数据：

    ```python
    """Database seed script for webLing.

    Usage:
        python scripts/seed.py

    Initializes:
        - Default character (ling) with Live2D config
        - Demo tenant with API key
    """
    import asyncio
    import os
    from motor.motor_asyncio import AsyncIOMotorClient

    MONGO_URI = os.getenv("MONGO_URI", "mongodb://localhost:27017")
    MONGO_DB = os.getenv("MONGO_DB", "webling")

    DEFAULT_CHARACTERS = [
        {
            "id": "ling",
            "name": "玲",
            "avatar_config": {
                "modelUrl": "/avatars/hiyori/hiyori_free_t08.model3.json",
                "scale": 0.9,
                "anchor": [0.5, 0.5],
                "motionMap": {},
                "autoBlink": True,
                "gazeMode": "mouse",
            },
            "greeting": "你好呀，我是玲。很高兴见到你。",
            "system_prompt": "你是玲，一个温柔可爱的虚拟助手。",
            "voice_id": None,
            "enabled": True,
        }
    ]

    DEFAULT_TENANTS = [
        {
            "id": "demo",
            "api_key_hash": "3a5ab5c1e2c0e69a1e0e7db4a9deafd6ac30b4b86b2c50d6f49e9d4f6cc0432a",
            "allowed_origins": ["http://localhost:5173", "http://127.0.0.1:5173"],
            "character_whitelist": ["ling"],
            "daily_quota": 2000,
            "scope": ["chat", "embed"],
        }
    ]

    async def seed_characters(db):
        """Insert default characters if not exists."""
        for char in DEFAULT_CHARACTERS:
            existing = await db.characters.find_one({"id": char["id"]})
            if existing is None:
                await db.characters.insert_one(char)
                print(f"  ✓ Created character: {char['id']}")
            else:
                print(f"  - Character already exists: {char['id']}")

    async def seed_tenants(db):
        """Insert demo tenant if not exists."""
        for tenant in DEFAULT_TENANTS:
            existing = await db.tenants.find_one({"id": tenant["id"]})
            if existing is None:
                await db.tenants.insert_one(tenant)
                print(f"  ✓ Created tenant: {tenant['id']}")
            else:
                print(f"  - Tenant already exists: {tenant['id']}")

    async def main():
        print(f"Connecting to MongoDB: {MONGO_URI}")
        client = AsyncIOMotorClient(MONGO_URI)
        db = client[MONGO_DB]

        # Test connection
        await client.admin.command("ping")
        print("✓ MongoDB connection successful\n")

        print("Seeding characters...")
        await seed_characters(db)

        print("\nSeeding tenants...")
        await seed_tenants(db)

        print("\n✓ Seed complete!")
        client.close()

    if __name__ == "__main__":
        asyncio.run(main())
    ```

    参考 D-07（seed 脚本初始化默认 Character + demo Tenant）
  </action>
  <verify>
    <automated>cd /Users/zert/Work/zert/lkl_code/webLing && python scripts/seed.py</automated>
  </verify>
  <done>seed 脚本可执行，默认 character（ling）和 demo tenant 已插入 MongoDB</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新 chat.ts store 支持 Character 列表和切换</name>
  <files>apps/web/src/stores/chat.ts</files>
  <behavior>
    - Test: fetchCharacters() 从 /api/characters 获取列表
    - Test: changeCharacter() 发送 change_character WS 事件
    - Test: currentCharacter 跟踪当前选中的 character
  </behavior>
  <action>
    更新 `apps/web/src/stores/chat.ts`，添加 Character 管理功能：

    1. **添加状态**：
       ```typescript
       const characters = ref<CharacterInfo[]>([]);
       const currentCharacter = ref<CharacterInfo | null>(null);
       ```

    2. **添加 Character 接口**（或从 @webling/core 导入）：
       ```typescript
       interface CharacterInfo {
         id: string;
         name: string;
         avatar_config: {
           modelUrl: string;
           scale?: number;
           anchor?: [number, number];
           motionMap?: Record<string, string>;
         };
         greeting: string;
         enabled: boolean;
       }
       ```

    3. **添加 fetchCharacters()**：
       ```typescript
       async function fetchCharacters(): Promise<void> {
         const response = await fetch('/api/characters');
         const data = await response.json();
         characters.value = data.characters;
         // 默认选中第一个
         if (characters.value.length > 0 && !currentCharacter.value) {
           currentCharacter.value = characters.value[0];
         }
       }
       ```

    4. **添加 changeCharacter()**：
       ```typescript
       async function changeCharacter(characterId: string): Promise<void> {
         const char = characters.value.find(c => c.id === characterId);
         if (!char) return;

         // 更新当前 character
         currentCharacter.value = char;

         // 如果已连接 WS，发送 change_character 事件
         if (socket && connection.value === 'open') {
           socket.send({ type: 'change_character', character_id: characterId });
         }

         // 触发 AvatarStage 重新 mount（不断 WS）
         // 这需要 AvatarStage 监听 currentCharacter 变化
       }
       ```

    5. **导出新状态和方法**：
       ```typescript
       return {
         // ... existing exports
         characters,
         currentCharacter,
         fetchCharacters,
         changeCharacter,
       };
       ```

    参考 D-09（ChatView 加角色选择器）、D-10（切换时发 change_character WS 事件）、D-11（不断 WS 连接）
  </action>
  <verify>
    <automated>cd /Users/zert/Work/zert/lkl_code/webLing && npx vue-tsc --noEmit apps/web/src/stores/chat.ts 2>&1 | head -20</automated>
  </verify>
  <done>chat.ts store 包含 characters, currentCharacter, fetchCharacters, changeCharacter</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现 CharacterPickerView 角色选择 UI</name>
  <files>apps/web/src/views/CharacterPickerView.vue</files>
  <behavior>
    - Test: 组件加载时调用 fetchCharacters()
    - Test: 显示 character 列表（卡片形式）
    - Test: 点击卡片调用 changeCharacter()
    - Test: 当前选中的 character 高亮显示
  </behavior>
  <action>
    更新 `apps/web/src/views/CharacterPickerView.vue`，实现角色选择 UI：

    ```vue
    <script setup lang="ts">
    import { onMounted } from 'vue';
    import { useChatStore } from '@/stores/chat';
    import { useRouter } from 'vue-router';

    const chat = useChatStore();
    const router = useRouter();

    onMounted(async () => {
      await chat.fetchCharacters();
    });

    async function selectCharacter(id: string) {
      await chat.changeCharacter(id);
      // 切换后返回主聊天界面
      router.push('/');
    }
    </script>

    <template>
      <section class="max-w-3xl mx-auto px-6 py-10">
        <h1 class="text-2xl font-bold mb-6">选择形象</h1>

        <div v-if="chat.characters.length === 0" class="text-slate-500">
          暂无可用形象
        </div>

        <div v-else class="grid grid-cols-2 md:grid-cols-3 gap-4">
          <button
            v-for="char in chat.characters"
            :key="char.id"
            @click="selectCharacter(char.id)"
            class="p-4 border rounded-lg hover:border-blue-500 transition-colors text-left"
            :class="{
              'border-blue-500 bg-blue-50': chat.currentCharacter?.id === char.id,
              'border-slate-200': chat.currentCharacter?.id !== char.id,
            }"
          >
            <div class="font-medium">{{ char.name }}</div>
            <div class="text-sm text-slate-500 mt-1">{{ char.id }}</div>
          </button>
        </div>
      </section>
    </template>
    ```

    参考 D-09（ChatView 加角色选择器）
  </action>
  <verify>
    <automated>cd /Users/zert/Work/zert/lkl_code/webLing && npx vue-tsc --noEmit apps/web/src/views/CharacterPickerView.vue 2>&1 | head -20</automated>
  </verify>
  <done>CharacterPickerView 显示角色列表，点击可切换 character</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>M7 Character 模块完整实现：MongoDB 迁移 + Character API + 前端角色切换</what-built>
  <how-to-verify>
    1. 启动后端：`cd backend && uvicorn app.main:app --reload`
    2. 运行 seed 脚本：`python scripts/seed.py`
    3. 启动前端：`cd apps/web && pnpm dev`
    4. 访问 http://localhost:5173
    5. 测试 API：`curl http://localhost:8000/api/characters`
    6. 在前端切换角色，检查 Live2D 形象是否变化
    7. 发送消息，检查对话是否正常
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. seed 脚本执行成功，默认 character 和 tenant 已插入
2. 前端角色选择 UI 正常显示
3. 切换角色时 Live2D 形象变化
4. WebSocket 连接在切换角色时保持不断
</verification>

<success_criteria>
- seed 脚本可初始化默认 Character（ling）
- 前端可显示角色列表并切换
- 切换角色时 AvatarStage 重新 mount 新模型
- 对话功能正常工作
</success_criteria>

<output>
After completion, create `.planning/phases/02-M7-Character-模块/02-03-SUMMARY.md`
</output>
