---
phase: 02-M7-Character-模块
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db.py
  - backend/app/models/character.py
  - backend/app/schemas/character.py
  - backend/app/routers/characters.py
  - backend/app/main.py
autonomous: true
requirements:
  - M7
must_haves:
  truths:
    - "MongoDB 连接在应用启动时建立"
    - "Character CRUD API 可正常访问"
    - "默认角色 ling 可通过 API 查询"
  artifacts:
    - path: "backend/app/db.py"
      provides: "MongoDB 连接管理"
      exports: ["get_db", "init_db"]
    - path: "backend/app/models/character.py"
      provides: "Character 数据模型"
      contains: "class Character"
    - path: "backend/app/schemas/character.py"
      provides: "Character API schemas"
      exports: ["CharacterCreate", "CharacterInfo", "CharacterList"]
    - path: "backend/app/routers/characters.py"
      provides: "Character CRUD API"
      exports: ["router"]
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/db.py"
      via: "lifespan init_db()"
      pattern: "init_db"
    - from: "backend/app/routers/characters.py"
      to: "backend/app/db.py"
      via: "get_db dependency"
      pattern: "get_db"
---

<objective>
实现 MongoDB 连接管理和 Character 模块的核心 API。

Purpose: 为 M7 Character 模块奠定数据层基础，提供 Character CRUD API。
Output: 可工作的 Character API（GET /api/characters, GET /api/characters/:id）
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-M7-Character-模块/02-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/config.py:
```python
class Settings(BaseSettings):
    mongo_uri: str = Field(default="mongodb://localhost:27017", alias="MONGO_URI")
    mongo_db: str = Field(default="webling", alias="MONGO_DB")
```

From packages/live2d-kit/src/avatar-config.ts:
```typescript
export interface AvatarConfig {
  modelUrl: string;
  scale?: number;
  anchor?: [number, number];
  motionMap?: Record<string, string>;
  autoBlink?: boolean;
  gazeMode?: 'mouse' | 'off';
  background?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 MongoDB 连接管理</name>
  <files>backend/app/db.py</files>
  <behavior>
    - Test: get_db() 返回 AsyncIOMotorDatabase 实例
    - Test: init_db() 连接到 MONGO_URI 指定的 MongoDB
    - Test: 无 Mongo 时降级到 None（dev fallback）
  </behavior>
  <action>
    创建 `backend/app/db.py`，使用 motor（async 原生驱动）管理 MongoDB 连接：

    1. **get_db()** 函数：
       - 从 app.state.db 获取数据库实例
       - 如果不存在，返回 None（dev fallback）

    2. **init_db(app: FastAPI)** 异步函数：
       - 从 Settings 获取 mongo_uri 和 mongo_db
       - 使用 motor.motor_asyncio.AsyncIOMotorClient 连接
       - 测试连接：await client.admin.command('ping')
       - 如果连接失败，记录警告，app.state.db = None（dev fallback）
       - 如果成功，app.state.db = client[mongo_db]

    3. **close_db(app: FastAPI)** 异步函数：
       - 关闭 motor client 连接

    使用 `from motor.motor_asyncio import AsyncIOMotorClient`
    参考 D-04（使用 motor 异步驱动）和 D-05（app.state.db 挂在 FastAPI 上）
  </action>
  <verify>
    <automated>cd backend && python -c "from app.db import get_db, init_db, close_db; print('imports ok')"</automated>
  </verify>
  <done>MongoDB 连接管理模块可导入，get_db/init_db/close_db 函数存在</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 Character 数据模型和 API</name>
  <files>backend/app/models/character.py, backend/app/schemas/character.py, backend/app/routers/characters.py</files>
  <behavior>
    - Test: Character 模型包含 id, name, avatar_config, greeting, system_prompt, voice_id, enabled 字段
    - Test: CharacterCreate schema 验证必填字段
    - Test: GET /api/characters 返回 CharacterList
    - Test: GET /api/characters/{character_id} 返回 CharacterInfo 或 404
  </behavior>
  <action>
    1. **创建 `backend/app/models/character.py`**：
       - 定义 Character dataclass：
         ```python
         @dataclass
         class Character:
             id: str
             name: str
             avatar_config: dict  # modelUrl, scale, anchor, motionMap 等
             greeting: str = "你好呀，我是玲。很高兴见到你。"
             system_prompt: str = ""
             voice_id: str | None = None
             enabled: bool = True
         ```
       - 参考 D-02 的字段定义

    2. **创建 `backend/app/schemas/character.py`**：
       - CharacterCreate(BaseModel): name, avatar_config, greeting, system_prompt, voice_id
       - CharacterInfo(BaseModel): id, name, avatar_config, greeting, system_prompt, voice_id, enabled
       - CharacterList(BaseModel): characters: list[CharacterInfo]

    3. **创建 `backend/app/routers/characters.py`**：
       - GET /api/characters: 查询 characters 集合，返回 CharacterList
       - GET /api/characters/{character_id}: 查询单个 character，404 如果不存在
       - 使用 get_db() 获取数据库连接
       - 如果 db 是 None，返回空列表或 404（dev fallback）
       - 参考 D-01（Character 全局共享）

    在 `backend/app/main.py` 中注册 router：
    ```python
    from app.routers import characters
    app.include_router(characters.router, prefix="/api")
    ```
  </action>
  <verify>
    <automated>cd backend && python -c "from app.models.character import Character; from app.schemas.character import CharacterCreate, CharacterInfo; from app.routers.characters import router; print('all imports ok')"</automated>
  </verify>
  <done>Character 模型、schemas 和 API 路由可导入，GET /api/characters 和 GET /api/characters/{id} 端点存在</done>
</task>

<task type="auto">
  <name>Task 3: 在 main.py 中集成 MongoDB 生命周期</name>
  <files>backend/app/main.py</files>
  <action>
    修改 `backend/app/main.py` 的 lifespan 函数，集成 MongoDB 初始化和关闭：

    ```python
    from app.db import init_db, close_db

    @asynccontextmanager
    async def lifespan(app: FastAPI) -> AsyncIterator[None]:
        settings = get_settings()
        inject_ling_path(settings.ling_repo_path)
        await init_db(app)  # 初始化 MongoDB 连接
        logger.info("webLing backend started (env=%s, port=%s).", settings.env, settings.port)
        yield
        await close_db(app)  # 关闭 MongoDB 连接
        logger.info("webLing backend stopped.")
    ```

    确保 characters router 已注册：
    ```python
    from app.routers import characters
    app.include_router(characters.router, prefix="/api")
    ```
  </action>
  <verify>
    <automated>cd backend && python -c "from app.main import app; print('app created ok')"</automated>
  </verify>
  <done>FastAPI 应用启动时自动初始化 MongoDB 连接，关闭时自动断开</done>
</task>

</tasks>

<verification>
1. 导入测试：所有新模块可正常导入
2. API 端点存在：GET /api/characters 和 GET /api/characters/{character_id}
3. MongoDB 连接：启动时尝试连接，失败时降级到 dev mode
</verification>

<success_criteria>
- MongoDB 连接管理模块（db.py）正常工作
- Character 数据模型和 schemas 定义完整
- Character API 路由注册并可访问
- 启动时自动初始化 MongoDB，关闭时自动断开
</success_criteria>

<output>
After completion, create `.planning/phases/02-M7-Character-模块/02-01-SUMMARY.md`
</output>
