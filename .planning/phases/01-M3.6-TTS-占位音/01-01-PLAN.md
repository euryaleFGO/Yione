---
phase: 01-M3.6-TTS-占位音
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/ws.py
  - packages/core/src/types/ws.ts
  - backend/app/ws/chat_ws.py
  - packages/live2d-kit/src/stage.ts
autonomous: true
requirements: [M3.6]
must_haves:
  truths:
    - "用户发消息后 Live2D 嘴型立即开始微动"
    - "TTS 首段音频播放时嘴型微动停止并由 lipsync 接管"
    - "不播放任何实际音频（静音 + 嘴型暗示）"
    - "用户打断时立即停止嘴型微动"
  artifacts:
    - path: "backend/app/schemas/ws.py"
      provides: "PlaceholderEvent 定义"
      contains: "class PlaceholderEvent"
    - path: "packages/core/src/types/ws.ts"
      provides: "PlaceholderEvent 前端类型"
      contains: "placeholder_mouth"
    - path: "backend/app/ws/chat_ws.py"
      provides: "占位事件发送逻辑"
      contains: "PlaceholderEvent"
    - path: "packages/live2d-kit/src/stage.ts"
      provides: "嘴型微动驱动方法"
      contains: "startPlaceholderMouth"
  key_links:
    - from: "backend/app/ws/chat_ws.py"
      to: "backend/app/schemas/ws.py"
      via: "PlaceholderEvent 实例化"
      pattern: "PlaceholderEvent\\("
    - from: "packages/live2d-kit/src/stage.ts"
      to: "packages/core/src/types/ws.ts"
      via: "PlaceholderEvent 类型引用"
      pattern: "PlaceholderMouth"
---

<objective>
实现 M3.6 TTS 占位嘴型效果：用户发消息后立即触发 Live2D 嘴型微动，TTS 首段音频播放时自动停止。

Purpose: 减少用户等待 TTS 首个音频的感知延迟。当前用户发消息后需等待 LLM 首 token + TTS 合成才能听到声音，期间完全静默。通过嘴型微动暗示"在听"，将感知首字延迟减半。

Output: 前后端 WS 协议扩展 + 后端事件发送 + 前端嘴型微动驱动
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-M3.6-TTS-占位音/01-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/schemas/ws.py:
```python
# Existing server events
class StateEvent(_ServerBase):
    type: Literal["state"] = "state"
    value: AgentState  # "listening" | "processing" | "speaking" | "idle"

class MotionEvent(_ServerBase):
    type: Literal["motion"] = "motion"
    name: str

ServerEvent = StateEvent | SubtitleEvent | MotionEvent | AudioEvent | ...
```

From packages/core/src/types/ws.ts:
```typescript
export type AgentState = 'listening' | 'processing' | 'speaking' | 'idle';

export type ServerEvent =
  | { type: 'state'; value: AgentState }
  | { type: 'motion'; name: string }
  | { type: 'audio'; url: string; segment_idx: number; sample_rate: number }
  | ...
```

From packages/live2d-kit/src/stage.ts:
```typescript
export interface AvatarControls {
  speak: (url: string, opts?: SpeakOptions) => Promise<void>;
  stopSpeaking: () => void;
  playMotion: (group: string, index?: number, priority?: number) => Promise<void>;
}

// Live2DInstance has:
//   speak?(sound, opts) - drives lipsync via audio analysis
//   stopSpeaking?() - stops current speak
//   motion?(group, index?, priority?) - plays motion group
```

From backend/app/ws/chat_ws.py:
```python
async def _handle_user_message(ws, user_text, character_id):
    # Current flow:
    # 1. Send StateEvent(value="processing")
    # 2. Create TTS worker
    # 3. Stream LLM response
    # 4. Queue sentences for TTS
    # 5. TTS worker sends AudioEvents
    # 6. Send StateEvent(value="idle") when done

async def _tts_worker(ws, pending, state):
    # Processes pending sentences
    # First segment detection: `first = True` flag
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 后端协议扩展 + 事件发送</name>
  <files>backend/app/schemas/ws.py, backend/app/ws/chat_ws.py</files>
  <read_first>
    - backend/app/schemas/ws.py (current WS schema definitions)
    - backend/app/ws/chat_ws.py (current turn handling flow)
  </read_first>
  <behavior>
    - Test: PlaceholderEvent(action="start") 序列化为 {"type": "placeholder_mouth", "action": "start"}
    - Test: PlaceholderEvent(action="stop") 序列化为 {"type": "placeholder_mouth", "action": "stop"}
    - Test: _handle_user_message 在 LLM 流式开始前发送 placeholder_mouth start
    - Test: _tts_worker 在首个 TTS segment 发送后发送 placeholder_mouth stop
    - Test: cancel 时不额外发送 stop（前端通过 StateEvent(idle) 推断）
  </behavior>
  <action>
    1. 在 backend/app/schemas/ws.py 中添加 PlaceholderEvent:
       ```python
       PlaceholderAction = Literal["start", "stop"]

       class PlaceholderEvent(_ServerBase):
           type: Literal["placeholder_mouth"] = "placeholder_mouth"
           action: PlaceholderAction
       ```
       并将其加入 ServerEvent union。

    2. 在 backend/app/ws/chat_ws.py 的 _handle_user_message 中:
       - 在 `await _send(ws, StateEvent(value="processing"))` 之后立即发送:
         `await _send(ws, PlaceholderEvent(action="start"))`
       - 这确保用户发消息后 <100ms 内前端收到占位信号

    3. 在 backend/app/ws/chat_ws.py 的 _tts_worker 中:
       - 在 `first = False` 之前（即首个 segment 发送后）发送:
         `await _send(ws, PlaceholderEvent(action="stop"))`
       - 这确保 TTS 音频开始播放时嘴型停止

    4. 在 backend/app/schemas/ws.py 的 __all__ 中添加 PlaceholderEvent
  </action>
  <verify>
    <automated>cd /Users/zert/Work/zert/lkl_code/webLing/backend && python -c "from app.schemas.ws import PlaceholderEvent; e = PlaceholderEvent(action='start'); print(e.model_dump(mode='json'))"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/schemas/ws.py contains "class PlaceholderEvent"
    - backend/app/schemas/ws.py contains 'Literal["placeholder_mouth"]'
    - backend/app/schemas/ws.py ServerEvent union includes PlaceholderEvent
    - backend/app/ws/chat_ws.py contains "PlaceholderEvent(action=\"start\")"
    - backend/app/ws/chat_ws.py contains "PlaceholderEvent(action=\"stop\")"
    - backend/app/ws/chat_ws.py start event is sent AFTER StateEvent(processing)
    - backend/app/ws/chat_ws.py stop event is sent AFTER first AudioEvent
  </acceptance_criteria>
  <done>PlaceholderEvent 定义完成，前后端 schema 同构，_handle_user_message 和 _tts_worker 正确发送占位事件</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 前端协议类型 + 嘴型微动驱动</name>
  <files>packages/core/src/types/ws.ts, packages/live2d-kit/src/stage.ts</files>
  <read_first>
    - packages/core/src/types/ws.ts (current WS type definitions)
    - packages/live2d-kit/src/stage.ts (current AvatarStage implementation)
  </read_first>
  <behavior>
    - Test: PlaceholderEvent 类型定义正确 (type: "placeholder_mouth", action: "start" | "stop")
    - Test: ServerEvent union 包含 PlaceholderEvent
    - Test: AvatarControls 接口新增 startPlaceholderMouth/stopPlaceholderMouth 方法
    - Test: startPlaceholderMouth 调用后 model 开始嘴型微动
    - Test: stopPlaceholderMouth 调用后嘴型微动停止
    - Test: startPlaceholderMouth 不会覆盖正在进行的 speak
  </behavior>
  <action>
    1. 在 packages/core/src/types/ws.ts 中添加 PlaceholderEvent 类型:
       ```typescript
       export type PlaceholderMouthAction = 'start' | 'stop';
       
       export interface PlaceholderMouthEvent {
         type: 'placeholder_mouth';
         action: PlaceholderMouthAction;
       }
       ```
       并将其加入 ServerEvent union。

    2. 在 packages/live2d-kit/src/stage.ts 中:
       - 在 AvatarControls 接口添加:
         ```typescript
         startPlaceholderMouth: () => void;
         stopPlaceholderMouth: () => void;
         ```
       - 在 AvatarStage 类中实现:
         ```typescript
         private placeholderTicker: (() => void) | null = null;
         
         startPlaceholderMouth(): void {
           if (this.placeholderTicker) return; // 已在运行
           const model = this.model;
           if (!model?.internalModel) return;
           
           const startTime = Date.now();
           this.placeholderTicker = () => {
             const elapsed = (Date.now() - startTime) / 1000;
             // 0.3Hz 正弦波，幅度 0.15（微动，不是大幅张合）
             const value = Math.sin(elapsed * 0.3 * Math.PI * 2) * 0.15;
             // 直接设置 ParamMouthOpenY
             const coreModel = model.internalModel?.coreModel;
             if (coreModel) {
               const paramIndex = coreModel.getParameterIndex('ParamMouthOpenY');
               if (paramIndex >= 0) {
                 coreModel.setParameterValueById('ParamMouthOpenY', value);
               }
             }
           };
           // 加入 PIXI ticker
           const app = this.appAny as { ticker?: { add(fn: () => void): void } } | null;
           app?.ticker?.add(this.placeholderTicker);
         }
         
         stopPlaceholderMouth(): void {
           if (!this.placeholderTicker) return;
           const app = this.appAny as { ticker?: { remove(fn: () => void): void } } | null;
           app?.ticker?.remove(this.placeholderTicker);
           this.placeholderTicker = null;
           // 重置嘴型到闭合
           const model = this.model;
           const coreModel = model?.internalModel?.coreModel;
           if (coreModel) {
             const paramIndex = coreModel.getParameterIndex('ParamMouthOpenY');
             if (paramIndex >= 0) {
               coreModel.setParameterValueById('ParamMouthOpenY', 0);
             }
           }
         }
         ```
       - 在 stopSpeaking() 中调用 stopPlaceholderMouth() 确保 speak 开始时占位停止
       - 在 unmount() 中调用 stopPlaceholderMouth() 清理资源

    3. 更新 AvatarControls 暴露逻辑（mount 后返回的 controls 对象）:
       ```typescript
       startPlaceholderMouth: () => this.startPlaceholderMouth(),
       stopPlaceholderMouth: () => this.stopPlaceholderMouth(),
       ```
  </action>
  <verify>
    <automated>cd /Users/zert/Work/zert/lkl_code/webLing/packages/live2d-kit && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/types/ws.ts contains "PlaceholderMouthEvent"
    - packages/core/src/types/ws.ts ServerEvent union includes PlaceholderMouthEvent
    - packages/live2d-kit/src/stage.ts AvatarControls interface has "startPlaceholderMouth"
    - packages/live2d-kit/src/stage.ts AvatarControls interface has "stopPlaceholderMouth"
    - packages/live2d-kit/src/stage.ts contains "startPlaceholderMouth()"
    - packages/live2d-kit/src/stage.ts contains "stopPlaceholderMouth()"
    - packages/live2d-kit/src/stage.ts contains "ParamMouthOpenY"
    - packages/live2d-kit/src/stage.ts stopSpeaking() calls stopPlaceholderMouth()
  </acceptance_criteria>
  <done>前端 PlaceholderEvent 类型定义完成，AvatarStage 实现嘴型微动驱动方法，与 speak/stopSpeaking 正确协调</done>
</task>

<task type="auto">
  <name>Task 3: 前端 WS 事件处理 + Chat Store 集成</name>
  <files>packages/core/src/stores/chat.ts</files>
  <read_first>
    - packages/core/src/stores/chat.ts (current WS event handling)
    - packages/core/src/types/ws.ts (updated ServerEvent types from Task 2)
  </read_first>
  <action>
    1. 在 packages/core/src/stores chat.ts 的 WS 消息处理逻辑中:
       - 添加对 `placeholder_mouth` 事件的处理:
         ```typescript
         case 'placeholder_mouth':
           if (event.action === 'start') {
             // 调用 AvatarControls.startPlaceholderMouth()
             avatarControls?.startPlaceholderMouth();
           } else if (event.action === 'stop') {
             // 调用 AvatarControls.stopPlaceholderMouth()
             avatarControls?.stopPlaceholderMouth();
           }
           break;
         ```

    2. 确保 avatarControls 引用可用（从 AvatarStage 组件获取）

    3. 在 StateEvent 处理中:
       - 当 value === 'idle' 时调用 stopPlaceholderMouth() 作为兜底
       - 这确保 cancel 后嘴型停止（D-07, D-08）

    4. 在 AudioEvent 处理中（如果有）:
       - 当收到第一个 AudioEvent 时调用 stopPlaceholderMouth()
       - 这是额外的保险，确保 TTS 开始时占位停止
  </action>
  <verify>
    <automated>cd /Users/zert/Work/zert/lkl_code/webLing/packages/core && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/stores/chat.ts contains "placeholder_mouth"
    - packages/core/src/stores/chat.ts contains "startPlaceholderMouth"
    - packages/core/src/stores/chat.ts contains "stopPlaceholderMouth"
    - packages/core/src/stores/chat.ts handles 'idle' state with stopPlaceholderMouth
  </acceptance_criteria>
  <done>前端 chat store 正确处理 placeholder_mouth 事件，avatarControls 集成完成，cancel/idle 兜底逻辑就位</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→WS | 前端接收后端 WS 事件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-01-01 | Tampering | PlaceholderEvent | accept | 事件由后端生成，前端仅响应，无注入风险 |
| T-01-02 | DoS | PlaceholderMouth ticker | mitigate | startPlaceholderMouth 检查是否已在运行，防重复注册 |
</threat_model>

<verification>
1. 后端 PlaceholderEvent 序列化测试通过
2. 前端 TypeScript 编译无错误
3. 前后端 WS schema 结构一致（PlaceholderEvent 在两端定义匹配）
4. 嘴型微动参数验证：频率 0.3Hz，幅度 0.15（D-02 适中幅度）
</verification>

<success_criteria>
- 用户发消息后 <100ms 内 Live2D 嘴型开始微动
- TTS 首段音频播放时嘴型微动停止，lipsync 自动接管
- 不播放任何实际音频（仅嘴型暗示）
- 用户打断（cancel/speech_start）后嘴型立即停止
- 嘴型微动幅度适中（0.15），不是大幅张合
</success_criteria>

<output>
After completion, create `.planning/phases/01-M3.6-TTS-占位音/01-01-SUMMARY.md`
</output>
