# TTS 模块核心依赖
# Python 3.12, CUDA 12.4, Windows

# PyTorch (通过 --index-url 安装，见 install_tts_dependencies.bat)
# torch==2.3.1
# torchvision==0.18.1
# torchaudio==2.3.1

# ONNX Runtime
onnxruntime==1.18.0
# 如果需要 GPU 支持: onnxruntime-gpu==1.18.0 (Windows 可能需要特殊配置)

# 核心依赖
numpy==1.26.4
librosa==0.10.2
soundfile==0.12.1
conformer==0.3.2
diffusers==0.29.0
modelscope==1.20.0
transformers==4.51.3
onnx==1.16.0

# 配置和工具
hydra-core==1.3.2
HyperPyYAML==1.2.2
ruamel.yaml==0.18.14  # 必需！HyperPyYAML 需要特定版本的 ruamel.yaml
ruamel.yaml.clib==0.2.12  # ruamel.yaml 的 C 库
omegaconf==2.3.0
lightning==2.2.4
pyworld==0.3.4
rich==13.7.1
gdown==5.1.0
wget==3.2
wetext==0.0.4

# 其他依赖
inflect==7.3.1
matplotlib==3.7.5
networkx==3.1
protobuf==4.25
pyarrow==18.1.0
pydantic==2.7.0
tensorboard==2.14.0
openai-whisper==20240930  # 必需！提供 whisper 模块

# Whisper 和 tokenizer 相关
tiktoken>=0.5.0  # Whisper tokenizer 需要（openai-whisper 的依赖）

# 云端 TTS 服务（Flask 队列/近似流式）
flask==3.0.3
flask-cors==4.0.1

# 免费基础 TTS（无需 key）
edge-tts==6.1.12

# 可选依赖
# ttsfrd  # Windows 上可能不支持，如果安装失败会使用 wetext 作为替代

# Windows 特定依赖（如果需要）
# kaldifst==1.7.14  # Windows 可能不支持，需要单独安装

flask-cors==4.0.1
