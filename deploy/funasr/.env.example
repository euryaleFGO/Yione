# 复制为 .env 并按需改写

# 宿主机暴露端口
FUNASR_PORT=10095

# 模型缓存目录（首次启动会下载 ~1.7GB ONNX 模型）
# 默认 ./models 相对 deploy/funasr/
# 生产可指向服务器统一模型目录，如：
# FUNASR_MODELS_DIR=/app/zert/funasr-online/models
FUNASR_MODELS_DIR=./models
